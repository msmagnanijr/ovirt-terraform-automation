# ovirt-terraform-automation
Usage example using the plugin https://github.com/oVirt/terraform-provider-ovirt

Every template needs to have cloud-init installed to work properly: yum install cloud-init -y
